# Copy this file to .env.local and fill in your Supabase project's values
# (Project Settings -> API in the Supabase dashboard). Both values are safe
# to expose in client-side code - do not put the "service_role" key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
